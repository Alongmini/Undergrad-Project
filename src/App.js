//import GoodsList from "./views/GoodsList";
//import Admin from "./views/Admin";
function App(props) {
  return (
    <div>
      {props.children}
    </div>
  )
}
export default App;

//props.children 拿到接收的组件内容
 